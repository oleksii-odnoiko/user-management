User manager app

## Quickstart

Intsall dependencies:
```sh
  npm run init
  ```

Run app in development mode:
```sh
  npm run start:dev
   ```

Build app for production:
```sh
  npm run build
   ```

Run app in production:
```sh
  npm run start
   ```
